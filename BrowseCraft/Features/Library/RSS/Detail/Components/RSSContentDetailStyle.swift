import SwiftUI

enum RSSContentDetailStyle {
    static let primaryTextColor: Color = Color(
        red: 25 / 255,
        green: 32 / 255,
        blue: 45 / 255
    )

    static let metricTextColor: Color = Color(
        red: 88 / 255,
        green: 89 / 255,
        blue: 91 / 255
    )

    static let pageBackgroundColor: Color = Color.white

    static let contentBackgroundColor: Color = Color(
        red: 252 / 255,
        green: 252 / 255,
        blue: 252 / 255
    )

    static let metadataBackgroundColor: Color = Color(
        red: 238 / 255,
        green: 238 / 255,
        blue: 238 / 255
    )
}
