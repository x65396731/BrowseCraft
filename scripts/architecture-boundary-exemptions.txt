# 中文注释：check-architecture-boundaries.sh 的豁免清单。每行「路径 标识」，路径相对本仓库根目录
# （包用 ../BrowseCraftXxx/...），标识是被引用的类型名或被禁用的记号（print( / try!）。
# 这里登记的是 2026-09-18 扩展闸门时已经存在的命中，目的是先把今天的取值固化成基线、阻止继续漂移；
# 每条都应逐个收敛后删除，不得新增未经审阅的条目。

# Features → Infrastructure：Features 直接构造基础设施类型，应改经 Application port 注入。
# 2026-09-18 已收敛：ImageCacheConfigurator 改经 Application 端口 ImageCacheManaging 注入；
# 两个 Bundled*AnimationResource 只是只读的 bundle 资源查找、无副作用，已归位到 Shared/Resources；
# EPUBNavigatorRepresentable 对 ReadiumBookEnvironment 的引用随「去掉本地 HTTP 服务」一并消失；
# 两个 Readium 类型改为由阅读器自己声明协议（BookReaderPublicationPorts.swift），符合性挂在装配根。
# 本方向已全部收敛，若再出现命中说明是新引入的，应当失败而不是登记豁免。

# Shared → Application / Shared → Infrastructure：2026-09-18 已全部收敛。
# 错误分类器下沉到 Application/Diagnostics（它要认 Application 的错误类型，且 Application 自己也用它）；
# 缩略图缓存池改经 Environment 注入（池子带 Nuke 类型，进不了 Application 端口）。
# 这两个方向已无豁免，若再出现命中说明是新引入的，应当失败而不是登记豁免。


# try!：字面量正则的 static let，模式固定不会失败；保留登记以防新增。
../BrowseCraftRuntime/Sources/BrowseCraftRuntime/Book/BookSourceRuntime.swift try!
